'use strict';

var _assert = require('assert');

var _assert2 = _interopRequireDefault(_assert);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function noOp(param) {
  return param;
}

function taggedTemplateLiterals(strings, values, callback) {
  (0, _assert2.default)(Array.isArray(values), 'Values should be an array');
  (0, _assert2.default)(Array.isArray(strings), 'Strings should be an array');

  var length = strings.length > values.length ? strings.length : values.length;
  var toReturn = [];
  var filterCallback = typeof callback === 'function' ? callback : noOp;
  for (var i = 0; i < length; ++i) {
    var string = strings[i];
    var value = values[i];
    if (string) {
      toReturn.push(string);
    }
    if (value) {
      toReturn.push(filterCallback(value));
    }
  }
  return toReturn.join('');
}

module.exports = taggedTemplateLiterals;