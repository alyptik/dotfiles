export declare namespace Touch {
    const None: 0;
    const First: 1;
    const Last: 2;
}
export declare type Touch = 0 | 1 | 2;
export declare class LinkedMap<T> {
    private map;
    private head;
    private tail;
    private _length;
    constructor();
    isEmpty(): boolean;
    readonly length: number;
    get(key: string): T | undefined;
    add(key: string, value: T, touch?: Touch): void;
    remove(key: string): T | undefined;
    shift(): T | undefined;
    private addItemFirst(item);
    private addItemLast(item);
    private removeItem(item);
    private touch(item, touch);
}
