function sslpkey() { [[ -d ./private ]] && (touch private/key.pem && chmod 0600 private/key.pem) || (mkdir -m0700 private && touch private/key.pem && chmod 0600 private/key.pem) openssl genpkey -algorithm RSA -out private/key.pem -pkeyopt rsa_keygen_bits:4096 #openssl genpkey -aes-256-cbc -algorithm RSA -out private/key.pem -pkeyopt rsa_keygen_bits:4096 openssl req -new -sha256 -key private/key.pem -out req.csr openssl req -key private/key.pem -x509 -new -days 3650 -out cacert.pem  }
